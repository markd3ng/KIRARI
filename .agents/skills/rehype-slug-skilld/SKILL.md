---
name: rehype-slug-skilld
description: "rehype plugin to add `id` attributes to headings. ALWAYS use when writing code importing \"rehype-slug\". Consult for debugging, best practices, or modifying rehype-slug, rehype slug."
metadata:
  version: 6.0.0
  generated_at: 2026-05-06
---

# rehypejs/rehype-slug `rehype-slug@6.0.0`
**Tags:** latest: 6.0.0

**References:** [package.json](./.skilld/pkg/package.json)

## Search

Use `skilld search "query" -p rehype-slug` instead of grepping `.skilld/` directories. Run `skilld search --guide -p rehype-slug` for full syntax, filters, and operators.

Related: unist-util-visit-skilld
